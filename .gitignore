# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Secrets / local config — NEVER COMMIT (hold live API keys)
.env
agent_config.yaml

# Runtime state (regenerated each run)
evergreen_memory.jsonl
clock/clock_state.json
